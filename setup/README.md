# setup

- config: Includes `config.yaml` for server to load environment variables.
- infra: Includes `docker-compose.yaml` and supported `.sh` files to set up infrastructure for local development.
- sql: Includes database migration files and `init.sql` to initialize the local database.